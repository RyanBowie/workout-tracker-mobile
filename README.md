# Workout Tracker Mobile

This is a minimal Expo + TypeScript scaffold for the Workout Tracker mobile app.

Getting started:

1. cd workout-tracker-mobile
2. npm install
3. npx expo start

This project includes React Navigation and a simple storage service using AsyncStorage.
